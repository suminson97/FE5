import { divide } from "lodash";
import React from "react";

const Home = () => {
  return (
    <div>
      <h1>this is home</h1>
      <button onClick={() => console.log("clicked")}>버튼입니다.</button>
    </div>
  );
};
export default Home;
